aws_region = "<AWS_REGION>"

aws_default_tags = {
  project = "<PROJECT>"
}

cluster_name = "<CLUSTER_NAME>"

ingress_class_name  = "default-ingress-class"
cluster_issuer_name = "default-cluster-issuer"

prometheus_host = "<PROMETHEUS_HOST>"
grafana_host    = "<GRAFANA_HOST>"
