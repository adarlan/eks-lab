name: Deploy

on:
  workflow_dispatch:

jobs:

  vpc-network:
    uses: .github/workflows/terraform-apply.yaml
    with:
      dir: vpc-network

  eks-cluster:
    needs: [vpc-network]
    uses: .github/workflows/terraform-apply.yaml
    with:
      dir: eks-cluster
