name: Deploy Applications 📦

on:
  workflow_dispatch:

jobs:

  argocd-applications:
    uses: ./.github/workflows/terraform-apply.yaml
    with:
      dir: argocd-applications
    secrets: inherit
